# 仓库信息（发布时直接复制粘贴用）

> 建仓时 GitHub 会问你几个字段，这里全部备好了，复制粘贴即可。
> **仓库名：`re-perception`**

---

## 一、仓库名（Repository name）

```
re-perception
```

**名字的来历**：`re` 是 reverse engineering 的缩写（也是 re- 这个前缀本身
「重新、再」的意思），`perception` 是「感知」。合起来是「重新感知」——
把一个看不懂的二进制，重新变成能被理解的东西。对 AI 来说，就是给它装上
一双能「看见」未知文件内部的眼睛。

---

## 二、简介 / Description（英文，GitHub 默认显示这个）

```
Give any AI agent the workflow of a real reverse engineer — identify, fingerprint,
disassemble and document any unknown file (PE / ELF / Mach-O / APK / DEX / pyc /
WASM / firmware / private formats). 26 CLI commands, 97 self-tests, zero third-party
dependencies, pure Python stdlib.
```

> GitHub 的 Description 限长 350 字符，上面这段 291 字符，刚好放得下。
> 完整版（写进 README 顶部的那种）：

```
Agent Skill: give any AI agent a real reverse engineer's workflow for any unknown
file — PE/ELF/Mach-O/APK/DEX/pyc/WASM/firmware. Zero third-party deps, pure stdlib.
```

---

## 三、简介 / Description（中文）

```
让任何 AI 智能体拥有真正逆向工程师的工作流 —— 识别、比对指纹、反汇编、成文，
覆盖 PE / ELF / Mach-O / APK / DEX / pyc / WASM / 固件 / 私有格式。
26 个子命令，97 条自检用例，零第三方依赖，纯 Python 标准库。
```

> GitHub 的 Description 字段**只能填一份**。如果你想中英都给，推荐这样写
> （185 字符，两种语言都出现）：

```
Agent Skill · AI 驱动的全平台逆向工程工具箱 —— 识别/反汇编/符号还原任何未知文件
(PE/ELF/Mach-O/APK/固件)。Zero third-party deps, pure stdlib.
```

---

## 四、Topics（最多 20 个，一行一个或空格分隔）

```
reverse-engineering
agent-skills
ai-agent
skill
claude-code
codex
malware-analysis
disassembler
binary-analysis
static-analysis
pe
elf
macho
apk
dex
firmware
security
infosec
python
no-dependencies
```

> 这 20 个都是检索量较高的标签。`no-dependencies` 和 `agent-skills` 是最能
> 让你在同类项目里被找到的两个。

---

## 五、其他设置建议

| 项目 | 建议 | 原因 |
|---|---|---|
| **Website** | 留空 | 没有独立站点，别放失效链接 |
| **Releases** | ✅ 勾选 | 要用它发 v1.3.8 |
| **Packages** | ✅ 勾选 | 同上 |
| **Wiki** | ❌ 不勾 | 文档已在仓库里，两处必然漂移 |
| **Issues** | ✅ 开启 | 已有 `.github/ISSUE_TEMPLATE/` 模板 |
| **Discussions** | 可选 | 想有问答区就开 |
| **License** | MIT | 已带 `LICENSE` 文件，GitHub 会自动识别 |

---

## 六、首发 Release

- **Tag**：`v1.3.8`
- **Title**：`v1.3.8 — 逐工具体检，再挖 7 处静默缺陷`
- **Description**：直接复制 `CHANGELOG.md` 里 `## [1.3.8]` 那一整段

---

## 七、语言占比提示

GitHub 会自动统计仓库语言。这个项目里 `.py` 占大头，所以会显示 **Python**。
`SKILL.md` 和文档都是 Markdown，不计入语言统计。这是正常的。
