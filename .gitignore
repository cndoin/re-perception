# 编译产物与缓存
__pycache__/
*.py[cod]
*.pyc
*.pyo

# 开发脚手架：_ref/ 放的是 demangle 开发期对照用的 LLVM 源码（版权归 LLVM
# 项目所有，体积也大），不随本仓库分发 —— 运行时**不会**读它，缺了不影响使用。
# 需要对照时直接从 LLVM 官方仓库取（llvm-project/lib/Demangle 下的
# MicrosoftDemangle.cpp 等），不必依赖本目录。
scripts/_dev/_ref/

# 自检与分析的临时产物
# （这些是 re.py / selftest.py 跑出来的东西。历史上真发生过
#   sample_pe64.exe.re-report.md 混进 scripts/ 跟着发布出去，
#   所以规则放宽到能覆盖所有分析产物后缀。）
_st_last.log
_lint.log
_st*.log
*.re-report.md
*.re-report.json
*.re-cfg.json
*.re-funcs.txt
*.re-strings.txt
re-selftest/
.re-selftest/

# 编辑器 / 系统
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# 虚拟环境
.venv/
venv/
env/
