# 手动上传到 GitHub · 保姆级步骤

> 如果你不想用命令行，照这个做，全程 5 分钟，一个命令都不用敲。
> **仓库名：`re-perception`**

---

## 方式 A：网页拖拽上传（最简单，但会丢提交历史）

### 第 1 步：建仓库

1. 打开 https://github.com/new
2. **Repository name** 填：`re-perception`
3. **Description** 填（复制 `仓库信息-复制粘贴用.md` 第二节里的英文简介）
4. 选 **Public**
5. **⚠️ 千万不要勾** "Add a README file" / "Add .gitignore" / "Choose a license"
   —— 这三样本仓库都已经有了，勾了会产生冲突
6. 点 **Create repository**

### 第 2 步：上传文件

创建完会自动跳到仓库首页，点中间那句
**"uploading an existing file"**（或右上角 `Add file` → `Upload files`）。

然后把 `reverse-engineering` 文件夹里的东西**全部拖进去**：

```
SKILL.md   README.md   README.en.md   INSTALL.md   INSTALL.en.md
CONTRIBUTING.md   CONTRIBUTING.en.md   SECURITY.md   SECURITY.en.md
USE-POLICY.md   USE-POLICY.en.md   CODE_OF_CONDUCT.md   CODE_OF_CONDUCT.en.md
PUBLISHING.md   PUBLISHING.en.md   CHANGELOG.md   LICENSE   Makefile
.editorconfig   .gitattributes   .gitignore
仓库信息-复制粘贴用.md   一键推送.py
assets/   references/   rules/   scripts/   .github/
```

> **注意**：网页拖拽**不支持文件夹**，`assets`、`references`、`rules`、
> `scripts`、`.github` 这 5 个目录需要先进去、再拖里面的文件。
> 或者用下面「方式 B」，能一次搞定且保留历史。

**❌ 不要上传** `.git` 文件夹（网页方式传不了，也不需要）。

### 第 3 步：提交

页面下方 **Commit changes** 留默认信息，点绿色按钮。

---

## 方式 B：用现成脚本（推荐，保留完整提交历史）

项目里已经放好了 `一键推送.py`。双击运行（或右键 → 用 Python 打开），
它会问你三件事：账号名、仓库名、公开/私有，然后全自动完成。

**如果提示没装 Python**：请先安装 Python 3.10 或更高版本，然后在仓库目录运行：

```
python 一键推送.py
```

它会自动帮你：
- 引导你登录 GitHub（浏览器点一下）
- 替换掉代码里所有 `OWNER/REPO`、`<你的GitHub账号>` 占位符
- 把分支从 `master` 改成 `main`
- 建仓库、推送全部 31 条提交
- 打 `v1.3.8` tag、建 Release

---

## 方式 C：GitHub Desktop（图形界面，保留历史）

1. 下载 https://desktop.github.com
2. `File` → `Add local repository` → 选 `reverse-engineering` 文件夹
3. 点右上角 **Publish repository**
4. 取消勾选 "Keep this code private"（如果要公开）
5. 名字填 `re-perception`，点 **Publish**

这个方式最省心，提交历史完整保留。

---

## 上传后必做的两件事

### 1. 填 Topics

仓库首页右侧点齿轮图标（Topics 旁边），把
`仓库信息-复制粘贴用.md` 第四节那 20 个标签逐个粘进去。

### 2. 发 Release

仓库首页右侧 **Releases** → `Create a new release`：
- Tag：`v1.3.8`
- Title：`v1.3.8 — 逐工具体检，再挖 7 处静默缺陷`
- 描述：复制 `CHANGELOG.md` 里 `## [1.3.8]` 那一整段

---

## 万一你用了方式 A，还有个小尾巴要收

方式 A 会丢掉 git 历史，所以你**不用**管提交历史的事。但有一处代码需要改：

打开 `.github/ISSUE_TEMPLATE/config.yml`，把里面的两处 `OWNER/REPO`
改成你的真实 `账号名/re-perception`，否则那两个联系链接是 404。

（如果你的账号名不是 `cndoin`，README 和 PUBLISHING 里也可能有
`<你的GitHub账号>` 这样的占位符，一并替换。）
