# 安全策略

## 报告漏洞

**请勿开公开 issue。** 本项目的 issue 是公开的，漏洞细节一旦提交就再也收不回来。

请走以下任一私密渠道：

| 渠道 | 地址 | 说明 |
|---|---|---|
| GitHub 私密咨询 | 仓库页 → Security → *Report a vulnerability* | 首选，可加密、可跟踪 |
| 邮件 | 见仓库主页 profile 的联系方式 | 备用 |

请在报告里尽量包含：

- 受影响版本（`python re.py --version` 的输出）
- 复现步骤与最小输入（如果输入是样本，**不要直接贴二进制**，给哈希与获取方式）
- 实际影响（能否导致代码执行 / 越权读文件 / 拒绝服务 / 结果被伪造成"成功"）
- 你期望的正确行为

**48 小时内**给出首次回复（收到即确认，不含修复）。修复上线后会在
`CHANGELOG.md` 与 GitHub Security Advisory 里致谢（除非你要求匿名）。

## 支持范围

只维护 **最新发布版本**。历史版本的问题，请先升到最新版确认是否仍存在。

## 本项目自身的威胁模型

这一点值得单独说明，因为本工具**专门用来处理不可信的输入**：它分析的
任意文件都可能是攻击者精心构造的。因此以下都是**安全问题**，不是普通 bug：

| 类别 | 具体表现 | 为什么算安全问题 |
|---|---|---|
| 解析器内存耗尽 | 畸形头部声明的表项数/RVA 导致超大分配 | 分析样本反被样本打挂，等于拒绝服务 |
| 解析器栈溢出 | 递归下降解析器（嵌套结构 / 规则特征树）被打成深度炸弹 | `RecursionError` 或进程崩溃 |
| 无界读取 | 读取长度取自文件内字段而未与文件实际长度交叉校验 | 内存暴涨、超长挂起 |
| 路径穿越 | `--out`/提取目录被 `../` 逃逸出预期范围 | 可写任意路径 |
| **假成功** | 解析失败却返回 `ok: true` 且结果看似完整 | 使用者会据此下错结论，比明确报错危险得多 |
| 命令注入 | 把目标文件名/内容拼进 `shell=True` 调用 | 直接代码执行 |

其中**"假成功"是本项目最看重的一类**。"失败被上报为成功"在本项目的严重
级别等同于远程代码执行 —— 一个说得通但完全错误的逆向结论，代价可能是
一次错误的安全决策。

## 已知的（刻意）行为，不算漏洞

- **不写目标文件、不打补丁、不生成注册码**：这是设计约束，不是缺失功能。
- **`doctor` 会执行外部工具**：那是探测工具链是否可用（`--version` 级别），
  只调用已知工具名，不拼用户输入。
- **分析需要读取目标文件**：只读打开。若目标本身是恶意的，风险由使用者的
  隔离环境承担，见 `references/legal.md`。
- **`_dev/` 目录下的脚本**：开发脚手架，不在支持范围内，但仍接受安全报告。

## 使用者的责任

本工具用于**你拥有或已获书面授权**的目标。恶意样本必须在隔离环境分析。
完整条款见 [LICENSE](LICENSE)；合规红线（中/美/欧盟）见 `references/legal.md`。
